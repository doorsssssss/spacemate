# 知光平台 → SpaceMate 可迁移功能参考

> 本文档分析知光（zhiGuang）项目中值得借鉴到 SpaceMate 的功能模块，以及"怎么迁"的思路。不改代码，只讲思路。

---

## 1. 两项目对比一览

| 维度 | 知光（zhiGuang） | SpaceMate |
|---|---|---|
| 定位 | 知识社区（UGC 内容、社交互动） | 空间管理平台（自习室/座位/预约） |
| 并发压力 | 高（Feed 流、百万级点赞计数） | 低~中（预约冲突是核心瓶颈） |
| 存储 | MyBatis + MySQL + Redis + Kafka + ES + OSS + Canal | MyBatis-Plus + MySQL + Redis + Caffeine |
| 认证 | JWT RS256 双令牌 | JWT RSA2048 双令牌（已有） |
| 缓存 | 三级（Caffeine + Redis 碎片化 + DB） | 两级（Caffeine + Redis 整包 + DB） |
| 异步 | Kafka 事件驱动 + Canal binlog | 无（靠 Spring 事务 + 同步调用） |

核心结论：知光的大部分设计是为高并发社交场景做的，**不能全搬**，但有几个模块的思想可以简化后落地到 SpaceMate。

---

## 2. 缓存层差异详解：为什么知光是"三级"？

### 2.1 你们层数一样，区别在 Redis 内部

```
知光 Feed 缓存结构：
  L1:  Caffeine（JVM 内，亚毫秒，TTL 15s）
  L2a: Redis idsKey  —— 只存当前页的帖子 ID 列表（List）
  L2b: Redis itemKey —— 每个帖子的元数据独立存储（String，多页共享复用）
  L2c: Redis hasMoreKey —— 是否有下一页（软缓存，TTL 10-20s 随机）
  L3:  MySQL（回源）

SpaceMate 空间列表缓存结构：
  L1:  Caffeine（JVM 内，TTL 30s）
  L2:  Redis 整包 JSON —— 整个空间列表序列化为一个大 String（TTL 60s）
  L3:  MySQL（回源）
```

知光的 Redis 层被拆成了**三个结构**（ids / item / hasMore），而不是一个整包 JSON。这是最核心的差异。

### 2.2 知光 Redis 碎片化好在哪

**整包缓存（SpaceMate 现状）的问题：**

- 管理员改了空间 A 的名字 → 整个 `spacemate:space:list:v1` 被删掉 → 下一个请求要重新从 DB 查出所有空间，重新序列化，重新写 Redis
- 空间 A 出现在所有用户的列表页里，但每次只改了一个字段，整个列表缓存就全量重建

**知光碎片化解决：**

- 一个内容被修改 → 反向索引 `feed:public:index:{postId}:{hourSlot}` 精确找到它出现在哪些页面 → 只失效相关页面，不波及其他
- `itemKey` 是多页复用的：帖子 A 出现在第 1 页和第 3 页，它的元数据只存一份
- `idsKey` 只存 ID，体积极小，重建成本低

### 2.3 SpaceMate 现在不需要碎片化，但可以引入其中三个技巧

| 技巧 | 知光来源 | 适用场景 | 复杂度 |
|---|---|---|---|
| **Single-flight 锁** | `KnowPostFeedServiceImpl:134` 的 `ConcurrentHashMap` + `synchronized` | 热门空间被大量并发访问时防缓存击穿 | 低（加 30 行） |
| **随机抖动 TTL** | `ThreadLocalRandom.current().nextInt(30)` | 防止多个 key 同一时刻集体过期导致回源雪崩 | 极低（改 1 行） |
| **空间详情独立缓存** | `feed:item:{postId}` 独立存储 | 已有！空间详情本身就是独立缓存的，保持即可 | 无需改动 |

---

## 3. 可迁移模块（按性价比排序）

### 3.1 HotKey 检测器 + Single-Flight 锁 — 性价比最高

**为什么要迁：**

考试季的热门自习室可能被大量用户同时点击"查看空间详情"或"查询可预约座位"。虽然 SpaceMate 的并发量远不及知光，但缓存击穿（同一时刻大量请求穿透 Redis 打到 DB）是通用的防护需求。

**知光源码参考：**
- `cache/hotkey/HotKeyDetector.java` — 160 行，零外部依赖，纯内存滑动窗口
- `knowpost/service/impl/KnowPostFeedServiceImpl.java` 第 134-148 行 — single-flight 锁模式

**迁移思路（HotKey）：**

1. 新建一个 `HotKeyDetector` 组件，抄知光的四个核心设计：
   - **固定分段滑动窗口**：窗口 60s，等分 6 段（每段 10s），用 `int[]` 数组 + `AtomicInteger` 游标循环
   - **`record(key)`**：对数组当前段做 `++`，无锁操作
   - **`heat(key)`**：遍历各段求和，O(segments) 即 O(6)
   - **`@Scheduled` 轮转**：每 10s 把游标前移，清零新段
2. 在 `ClientSpaceServiceImpl.detail()` 和 `ClientSeatServiceImpl.available()` 里调用 `hotKey.record(key)`
3. 根据热度等级动态延长 Redis TTL：
   - NONE（<50 次/窗口）：基准 TTL，不做处理
   - LOW（50-200）：基准 +20s
   - MEDIUM（200-500）：基准 +60s
   - HIGH（>500）：基准 +120s

**迁移思路（Single-flight）：**

1. 在 `ClientSpaceServiceImpl` 或 `ClientSeatServiceImpl` 里加一个 `ConcurrentHashMap<String, Object> singleFlight`
2. 回源 DB 的代码段用 `synchronized(lock)` 包裹，锁内优先重查 Redis
3. 参考知光 feed 服务的模式：
   ```java
   // 伪代码（不修改，只说明思路）
   Object lock = singleFlight.computeIfAbsent(key, k -> new Object());
   synchronized (lock) {
       // 重查 Redis（可能其他线程已经写入）
       cached = redis.get(key);
       if (cached != null) return cached;
       // 只有第一个线程真正回源
       data = mapper.selectList(...);
       redis.set(key, data, ttl);
       return data;
   }
   ```

**落地成本：** HotKeyDetector 约 160 行 + single-flight 约 30 行，纯 Java，不引入新依赖。

---

### 3.2 看板统计预计算 — 异步写聚合简化版

**为什么要迁：**

SpaceMate 的看板（今日概览：总预约数 / 待使用 / 已使用 / 已取消 / 占用率）目前大概率是实时 `SELECT COUNT(*)` 或内存过滤。数据量增长后每次打开看板都跑聚合查询会越来越慢。

**知光思路：** 操作发生时异步累加 → 聚合桶暂存 → 定时刷入 Redis 摘要 → 读时 O(1)。

**但不需要 Kafka 和位图**，SpaceMate 可以用更简单的方案。

**知光源码参考：**
- `counter/event/CounterAggregationConsumer.java` — Kafka 消费 + Redis Hash 聚合 + 定时刷写 Lua
- `counter/service/impl/CounterServiceImpl.java` 第 419-440 行 — 管道化 BITCOUNT 重建

**迁移思路（简化版）：**

1. **写路径**：预约状态变更时（创建/核销/取消），用 Spring `ApplicationEventPublisher` 发一个本地事件，监听器异步更新 Redis Hash 里的当日看板计数：
   ```
   dashboard:{date}:{spaceId}
     total_bookings: 42
     pending:  15
     used:     20
     cancelled: 7
   ```
   用 `HINCRBY` 原子增减，不需要 Lua。

2. **读路径**：看板接口直接读这个 Redis Hash，实时 O(1)。

3. **校准任务**：`@Scheduled(fixedDelay=60000)` 每 60s 从 DB 跑一次 `COUNT(*) GROUP BY status` 覆盖 Redis，纠正可能的不一致。这就是知光"基于事实重建"的思想简化版。

4. **容错**：如果 Redis Hash 不存在或异常，回退到 DB 实时查询，查完顺手写回 Redis。参考知光 CounterServiceImpl 第 147-212 行的"缺失时自动重建"逻辑。

**落地成本：** 1 个事件监听器 + 1 个定时校准任务 + 1 个 Redis Hash，不引入新中间件。

---

### 3.3 预约冲突检测 Lua 原子化 — 消除并发窗口

**为什么要迁：**

SpaceMate 的预约冲突检测目前是：查 DB → 判断无冲突 → 插入。这是典型的 check-then-act 竞态：两个用户同时请求最后一个可用座位，两个请求都通过了冲突检查，然后都插入成功。

**知光思路：** 在关键竞态路径上用 Redis Lua 脚本做原子操作。

**知光源码参考：**
- `counter/service/impl/CounterServiceImpl.java` 第 443-458 行 — `TOGGLE_LUA`：`GETBIT` + 条件 `SETBIT` 原子执行
- 整体贯穿：知光在所有 Redis 临界区都用 Lua，不信任应用层的 check-then-act

**迁移思路：**

1. 在创建预约时，在 `booking` 表的 `seat_id` + `start_at ~ end_at` 时间段上加一个 Redis 占位 key：
   ```
   booking:seat:{seatId}:{timeSlot}: 1
   ```
   TTL = 预约持续时长。

2. 用 Lua 脚本原子执行：
   ```
   -- KEYS[1] = booking:seat:123:14:00-16:00
   -- ARGV[1] = bookingNo
   -- ARGV[2] = TTL
   local exists = redis.call('EXISTS', KEYS[1])
   if exists == 1 then return 0 end   -- 已有预约，冲突
   redis.call('SET', KEYS[1], ARGV[1], 'EX', ARGV[2])
   return 1   -- 占位成功
   ```

3. 占位成功后，再执行 DB 写入和 Redis 可预约缓存版本号递增。

4. 预约取消时，删除对应的 Redis 占位 key + 删除可预约缓存 + 递增版本号。参考知光 CounterServiceImpl 的 Lua 脚本模式来做原子释放。

**为什么不用 `SELECT ... FOR UPDATE` 悲观锁：**
- Redis Lua 不占用 MySQL 连接，不阻塞其他查询
- 如果以后部署多实例，Redis 是天然的跨实例锁
- 冲突时直接返回"座位已被占用"，不等待

**落地成本：** 1 个 Lua 脚本 + 预约创建流程中插入 Redis 占位逻辑，另外 `CacheInvalidationService` 已有的版本号递增机制可以复用。

---

### 3.4 AI 问答 — 轻量 RAG

**为什么要迁：**

SpaceMate 的设计文档已经预留了 `POST /api/v1/chat` 接口但未实现。用户可能问"这个自习室有 WiFi 吗？""现在还有安静座位吗？""开放时间是？"。SpaceMate 的结构化数据恰好适合做轻量 RAG。

**知光源码参考：**
- `llm/rag/RagQueryService.java` — 检索 + 构造 Prompt + SSE 流式输出
- `llm/rag/RagIndexService.java` — 内容抓取 + Markdown 切片 + 指纹去重 + 向量存储

**迁移思路（简化版，不需要向量存储）：**

SpaceMate 的空间信息是**结构化数据**（时间、设施、规则），不是长文。不需要 embedding 和向量检索。

1. **构造上下文**：把空间详情（名称、开放时间、WiFi、规则、价格、当前可用座位数）拼成一段 Markdown 文本，作为 system prompt 的一部分。

2. **对话**：直接用 Spring AI + DeepSeek，用户提问时：
   - 查一次 Redis/DB 拿空间最新数据
   - 拼成 system prompt："你是 SpaceMate 助手。只能依据以下空间信息回答：{空间数据}"
   - SSE 流式返回结果

3. **如果以后有长文本**（比如空间规则 2000 字、公告），再考虑引入知光的 `RagIndexService` 方案：Markdown 标题感知切片（800 字符 + 100 重叠）+ Elasticsearch 向量存储 + 指纹去重。

**落地成本：** 1 个 Controller + 1 个 Service，复用 Spring AI（SpaceMate 已有 DeepSeek 依赖就绪）。不需要 ES 向量库。

---

### 3.5 管理端认证 — JWT 统一

**现状：**

SpaceMate 管理端用的是 `X-Admin-Token: 123456` 固定令牌 + `AdminAuthInterceptor` 硬编码校验。新认证代码已经用了 JWT（RSA 2048），但管理端没接入同一套体系。

**知光源码参考：**
- `auth/config/SecurityConfig.java` — `@EnableMethodSecurity` + `OAuth2ResourceServer` + 公开/认证端点分离
- `auth/token/RedisRefreshTokenStore.java` — Refresh Token 白名单 + 撤销

**迁移思路：**

1. 在用户表已有的 `role` 字段（1=用户, 9=管理员）基础上，JWT payload 里加上 `role: admin` claim
2. 管理端接口去掉 `AdminAuthInterceptor`，改用 `@PreAuthorize("hasRole('admin')")` 保护
3. 管理端前端登录改为调用 `/api/v1/auth/login`，获取 JWT，后续请求带 `Authorization: Bearer <access_token>`
4. 管理员的 Refresh Token 同样存 Redis 白名单，支持单点撤销

**落地成本：** 中等。涉及管理端前端改动（login.html、app.js 的 token 存储和 Header 注入），后端主要是去掉旧拦截器 + 加 `@PreAuthorize`。

---

## 4. 不建议迁移的模块及原因

| 知光模块 | 为什么不迁 |
|---|---|
| **分片位图 + SDS 计数系统** | 为百万用户点赞场景设计。SpaceMate 的预约量级（几百条/天）用 Redis `HINCRBY` 或 `INCR` 足够，移植过来是严重的过度设计 |
| **Outbox + Canal + Kafka 事件驱动** | 引入 Kafka + Canal 运维成本极高（部署、监控、故障恢复）。SpaceMate 的数据一致性靠 Spring 事务 + `TransactionSynchronization` 后清缓存已经够稳 |
| **Feed 流三级缓存碎片化** | SpaceMate 没有 Feed 流需求。空间列表数据量小（几十个空间），整包 JSON 缓存完全够用。碎片化拆成 idsKey/itemKey 的收益在数据量上来之后才值得做 |
| **Elasticsearch 搜索** | SpaceMate 的空间和座位数量有限（几十到几百），MySQL 索引 + Redis 缓存足够。维护额外 ES 集群的投入产出比极低 |

---

## 5. 推荐迁移优先级

```
第一轮（立即，低风险高收益）：
  1. Single-flight 锁 — 防热门空间缓存击穿，30 行代码，零风险
  2. 随机抖动 TTL — 当前固定 60s/30s 改为 + random(0-30s)，改一行数字

第二轮（中期，有明确收益）：
  3. HotKey 检测器 — 160 行独立组件，考试季热门自习室生效
  4. 看板预计算 — 简化版异步聚合，让看板接口从 O(n) 降到 O(1)
  5. 预约 Lua 原子化 — 消除并发冲突窗口，直接解决业务正确性问题

第三轮（按需求触发）：
  6. AI 问答 — 用户端有价值，但需要接入 DeepSeek 和设计交互
  7. 管理端 JWT 统一 — 涉及前后端联调，改动面较大，适合在认证模块重构时一并做
```
